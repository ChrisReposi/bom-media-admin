Options -MultiViews

RewriteEngine On
RewriteBase /

# Giữ nguyên file và thư mục tồn tại thật
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Mọi route SPA còn lại trả về index.html
RewriteRule ^ index.html [L]
