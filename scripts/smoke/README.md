# Smoke Script Templates

These files are examples only. They are not wired into `package.json` and do not prove production smoke tests were run.

Use them as safe starting points for operator-managed checks:

- `api-smoke.example.sh` checks API health/docs/auth endpoint behavior with placeholders.

Do not store credentials or tokens in these scripts.

